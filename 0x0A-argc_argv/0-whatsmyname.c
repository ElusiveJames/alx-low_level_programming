#include "main.h"
/**
* main - check the code
* @argc: number of arguments
* @argv: array of arguments
*
* Return: Always 0.
*/
int main(int argc, char *argv[])
{
	if (argc < 2)
		printf("%s\n", argv[0]);
	return (0);
}
